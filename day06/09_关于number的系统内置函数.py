# python提供了一些用于运算的内置的函数

# 求最大值最小值
print(max(1, 2, 3, 4, 5, 3, 2, 1))
# 求最小值
print(min(1, 2, 3, 4, 5, 3, 2, 1))
# 求和
print(sum([1, 2, 3, 4, 5]))
# 求幂
print(pow(4, 2))
# 绝对值
print(abs(4))


a = 10
# 一个占有多字节的变量，其内存地址，是它所占用的多个字节的地址编号最小的那个地址。
print(id(a))
